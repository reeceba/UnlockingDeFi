const SUPABASE_URL='https://dvpxlirjmyluwljivtdw.supabase.co';
const SUPABASE_KEY='sb_publishable_E4GiDJ-pJ5n2a0WGBpHOdQ_upqwyiCA';
const LIVE_URL='https://reeceba.github.io/UnlockingDeFi/';
const supabaseClient=window.supabase?.createClient(SUPABASE_URL,SUPABASE_KEY);
const toast=document.getElementById('toast'),authShell=document.getElementById('authShell'),setupShell=document.getElementById('setupShell'),dashboardShell=document.getElementById('dashboardShell'),authStatus=document.getElementById('authStatus'),resetModal=document.getElementById('resetModal');
let authMode='signup';
function message(text,error=false){if(authStatus){authStatus.textContent=text;authStatus.style.color=error?'#ff8f9c':'#bd91ff'}if(toast){toast.textContent=text;toast.classList.add('show');clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>toast.classList.remove('show'),3200)}}
function showOnly(section){[authShell,setupShell,dashboardShell].forEach(el=>el?.classList.add('hidden'));section?.classList.remove('hidden')}
function initials(name){return(name||'Explorer').trim().charAt(0).toUpperCase()}
async function loadUser(){if(!supabaseClient)return;const{data:{session}}=await supabaseClient.auth.getSession();if(!session){showOnly(authShell);return}const{data:profile,error}=await supabaseClient.from('profiles').select('*').eq('id',session.user.id).maybeSingle();if(error||!profile){showOnly(setupShell);return}renderDashboard(profile);showOnly(dashboardShell)}
function renderDashboard(profile){const name=profile.username||'Explorer',goal=profile.learning_goal||'Solana basics';document.querySelectorAll('#dashName,#savedName').forEach(el=>el.textContent=name);const w=document.getElementById('welcomeName');if(w)w.textContent=name;const x=document.getElementById('savedExperience');if(x)x.textContent=profile.experience||'Complete beginner';const g=document.getElementById('dashGoal');if(g)g.textContent=goal;const gs=document.getElementById('goalStat');if(gs)gs.textContent=goal;document.querySelectorAll('#dashAvatar,#dashAvatarTop').forEach(el=>el.textContent=initials(name));const xp=document.getElementById('dashXp');if(xp)xp.textContent=profile.xp??0;const st=document.getElementById('dashStreak');if(st)st.textContent=profile.streak??0;const lv=document.getElementById('dashLevel');if(lv)lv.textContent=`${profile.current_level||1} / 10`}
document.querySelectorAll('.auth-tab').forEach(tab=>tab.addEventListener('click',()=>{authMode=tab.dataset.mode;document.querySelectorAll('.auth-tab').forEach(t=>t.classList.remove('active'));tab.classList.add('active');const signup=authMode==='signup';document.getElementById('authTitle').textContent=signup?'Start your journey.':'Welcome back.';document.getElementById('authCopy').textContent=signup?'Create an account and keep your learning progress wherever you go.':'Log in to pick up exactly where you left off.';document.getElementById('authSubmit').innerHTML=signup?'Create account <span>→</span>':'Log in <span>→</span>';document.getElementById('password').autocomplete=signup?'new-password':'current-password';if(authStatus)authStatus.textContent=''}));
document.getElementById('authForm')?.addEventListener('submit',async e=>{e.preventDefault();if(!supabaseClient)return message('Authentication is unavailable right now.',true);const email=document.getElementById('email').value.trim(),password=document.getElementById('password').value,submit=document.getElementById('authSubmit');submit.disabled=true;submit.textContent=authMode==='signup'?'Creating account…':'Logging in…';if(authMode==='signup'){const existing=await supabaseClient.auth.signInWithPassword({email,password});if(!existing.error&&existing.data?.session){message('That email already has an account. You are logged in.');await loadUser()}else{const{data,error}=await supabaseClient.auth.signUp({email,password,options:{emailRedirectTo:LIVE_URL}});if(error)message(error.message,true);else if(data.session){message('Account created. Let’s build your profile.');showOnly(setupShell)}else message('If this email is new, check your inbox to confirm. If you already have an account, use Log in or Forgot your password.')}}else{const{error}=await supabaseClient.auth.signInWithPassword({email,password});if(error)message(error.message,true);else{message('Welcome back!');await loadUser()}}submit.disabled=false;submit.innerHTML=authMode==='signup'?'Create account <span>→</span>':'Log in <span>→</span>'});
document.getElementById('forgotPassword')?.addEventListener('click',()=>{resetModal?.classList.remove('hidden');const email=document.getElementById('email')?.value.trim();if(email)document.getElementById('resetEmail').value=email});document.getElementById('closeReset')?.addEventListener('click',()=>resetModal?.classList.add('hidden'));
document.getElementById('resetForm')?.addEventListener('submit',async e=>{e.preventDefault();const email=document.getElementById('resetEmail').value.trim(),button=document.getElementById('resetSubmit');button.disabled=true;button.textContent='Sending…';const{error}=await supabaseClient.auth.resetPasswordForEmail(email,{redirectTo:LIVE_URL});if(error)message(error.message,true);else{resetModal.classList.add('hidden');message('If an account exists for that email, a secure reset link has been sent. Check your inbox.')}button.disabled=false;button.innerHTML='Send reset link <span>→</span>'});
document.querySelectorAll('.goal').forEach(goal=>goal.addEventListener('click',()=>{document.querySelectorAll('.goal').forEach(g=>g.classList.remove('selected'));goal.classList.add('selected')}));
document.getElementById('profileForm')?.addEventListener('submit',async e=>{e.preventDefault();const{data:{user}}=await supabaseClient.auth.getUser();if(!user){showOnly(authShell);return}const username=document.getElementById('name').value.trim()||'Explorer',experience=document.getElementById('experience').value,learning_goal=document.querySelector('.goal.selected')?.dataset.goal||'Solana basics',button=e.target.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Saving…';const{data:profile,error}=await supabaseClient.from('profiles').upsert({id:user.id,username,experience,learning_goal},{onConflict:'id'}).select().single();if(error){message('Profile could not be saved yet.',true);button.disabled=false;button.innerHTML='Save my profile <span>→</span>';return}localStorage.setItem('ud_profile',JSON.stringify(profile));renderDashboard(profile);showOnly(dashboardShell);message(`Welcome to UnlockingDeFi, ${username}!`)});
async function signOut(){await supabaseClient?.auth.signOut();showOnly(authShell);message('You have been signed out.')}document.getElementById('signOut')?.addEventListener('click',signOut);document.getElementById('dashboardSignOut')?.addEventListener('click',signOut);
supabaseClient?.auth.onAuthStateChange((event)=>{if(event==='PASSWORD_RECOVERY'){showOnly(authShell);resetModal?.classList.remove('hidden');const card=resetModal?.querySelector('.reset-card');if(card){card.querySelector('.eyebrow').textContent='PASSWORD RESET';card.querySelector('h2').textContent='Choose a new password.';card.querySelector('p:not(.eyebrow)').textContent='Enter and confirm your new password.';card.querySelector('form').innerHTML='<label>New password<input id="newPassword" type="password" minlength="6" autocomplete="new-password" placeholder="At least 6 characters" required></label><label>Confirm password<input id="confirmPassword" type="password" minlength="6" autocomplete="new-password" placeholder="Repeat your password" required></label><button class="primary-button full" type="submit" id="newPasswordSubmit">Update password <span>→</span></button>';document.getElementById('resetForm')?.addEventListener('submit',async ev=>{ev.preventDefault();const p=document.getElementById('newPassword').value,c=document.getElementById('confirmPassword').value;if(p!==c){message('Passwords do not match.',true);return}const b=document.getElementById('newPasswordSubmit');b.disabled=true;b.textContent='Updating…';const{error}=await supabaseClient.auth.updateUser({password:p});if(error){message(error.message,true);b.disabled=false;b.innerHTML='Update password <span>→</span>'}else{resetModal.classList.add('hidden');message('Password updated successfully.');await loadUser()}})}}else setTimeout(loadUser,0)});loadUser();